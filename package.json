{
  "name": "sentinal-mt5",
  "version": "1.0.0",
  "private": true,
  "description": "Sentinal MT5 - XAUUSD algorithmic trading platform with multi-position execution",
  "workspaces": [
    "shared",
    "engine",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace shared && npm run build --workspace engine && npm run build --workspace server && npm run build --workspace web",
    "start": "npm run start --workspace server",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace engine"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
